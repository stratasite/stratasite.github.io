---
/**
 * Product page: AI Analytics.
 *
 * Thesis: don't bolt one chatbot onto your warehouse. Non technical experts
 * build purpose built agents (the same way they build reports), grounded in
 * live entities and real data values, on a governed model that makes
 * correctness structural. Antagonist: the cold, chat box first agent that
 * starts every question from zero and guesses SQL.
 *
 * Reader: the data / AI leader whose agent over data pilot is returning wrong
 * answers, with the non technical builder as the empathy figure. Claims kept
 * inside STRATA.md §9 guardrails (grain checked / refuses, not "never wrong").
 */
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import ProductHero from '../../components/product/ProductHero.astro';
import DayInLife from '../../components/product/DayInLife.astro';
import CapabilityGrid from '../../components/product/CapabilityGrid.astro';
import TradeoffPanel from '../../components/product/TradeoffPanel.astro';
import OperatorTable from '../../components/product/OperatorTable.astro';
import CaseStudySpotlight from '../../components/CaseStudySpotlight.astro';
import VideoLightbox from '../../components/product/VideoLightbox.astro';
import CtaButton from '../../components/CtaButton.astro';

const caseStudies = (await getCollection('casestudies', ({ data }) => !data.draft)).sort(
  (a, b) => a.data.order - b.data.order
);
const featuredStudy = caseStudies[0];
const studyHref = featuredStudy ? `/case-studies/${featuredStudy.slug}/` : undefined;

const steps = [
  {
    time: 'Build',
    title: 'Build an agent the way you build a report',
    body: [
      'Open the same editor your team already uses for reports and assemble an agent instead. No code, no prompt engineering. If someone can build a report in Strata, they can build an agent.',
      'You drop live semantic entities and real, current data values straight into its instructions. Its context is not a paragraph describing your business, it is the actual governed measures, dimensions, and values it will reason over, inline and always current. Describe what you want in plain language and Strata resolves it to those governed fields, never a column the model invented.',
    ],
    underHood: {
      text: 'Purpose built agents on the governed semantic model, grounded in live entities and real data values, scoped to the slice that matters.',
      href: '/features/ai-native/',
      label: 'AI ready analytics',
    },
    image: '/ai_editor.png',
    alt: 'Building a purpose built agent in the Strata agent editor, with live entities and data values embedded in its instructions',
  },
  {
    time: 'Ground',
    title: 'Grounded in your data, not ground zero',
    body: [
      'A chat box over the warehouse starts every question cold, rediscovering what is relevant on every task. A Strata agent starts with the important subset already in hand, the exact measures, dimensions, and live values its job depends on.',
      'That changes two things at once. The agent understands your data in far richer detail, because the real definitions and examples sit right in its instructions. And it stays scoped to the slice that matters, instead of thrashing across a hundred tables trying to rediscover what is relevant.',
    ],
    underHood: {
      text: 'Natural language field and filter resolution grounds each agent in governed entities, so its context is real and current rather than a description the model has to trust.',
      href: '/features/ai-native/',
      label: 'AI ready analytics',
    },
    video: '/ai_sprinkled_editor.mp4',
    alt: 'Resolving plain language to governed semantic entities in the Strata editor',
  },
  {
    time: 'Trust',
    title: 'It navigates a validated space, never guesses SQL',
    body: [
      'Because the agent queries through the semantic model, it does not write SQL and hope. Every step returns the same contract: is this partial query valid, what are the compatible next steps, and if something is rejected, exactly why. The agent moves through a checked space instead of guessing joins.',
      'When it reaches for something the model cannot answer, Strata refuses before execution and hands back the nearest valid alternatives. The worst case is a missed answer the agent is told about and can route around, never a confident wrong number.',
    ],
    aside:
      'The interactive loop that validates a person\'s query as they build it is, structurally, the agent API. The feedback that makes the editor pleasant for people is the exact mechanism that makes it safe for machines.',
    underHood: {
      text: 'A compatibility matrix derived from each measure\'s fact, grain, and reachable dimensions decides what is answerable, and refuses the rest before anything runs.',
      href: '/features/grain-safe-blending/',
      label: 'Correctness by construction',
    },
    video: '/instant_feedback.mp4',
    alt: 'A query validated live as it is built, the same checked contract an agent moves through',
  },
];

const capabilities = [
  {
    icon: 'shield',
    title: 'Inherits your security',
    body: 'Agents query through the same layer as people, so row level security and masking apply automatically. An agent can never read what its user could not, and you never keep a second security model for the machines.',
  },
  {
    icon: 'layers',
    title: 'One model, one answer',
    body: 'Humans and agents reason over the same governed model. Build a report or build an agent, the number comes out the same, because both are answered the same governed way.',
  },
  {
    icon: 'gauge',
    title: 'Fast enough to actually use',
    body: 'A single agent task can fan out into dozens of queries. Federation and aggregate tables keep those turns sub second, so an agent rollout does not stall on latency.',
    href: '/features/federated-olap/',
  },
];

const compareRows = [
  {
    axis: 'Where each question starts',
    them: 'From zero, guessing your schema',
    strata: 'With your entities and live data already in hand',
  },
  {
    axis: 'Who builds it',
    them: 'Engineers, in prompts and pipelines',
    strata: 'The domain expert, in the report editor',
  },
  {
    axis: 'How it forms a query',
    them: 'Guesses SQL and joins',
    strata: 'Navigates a validated space, never guesses',
  },
  {
    axis: 'When it cannot answer',
    them: 'A confident wrong number',
    strata: 'Refuses, with the nearest valid alternative',
  },
  {
    axis: 'What it is allowed to see',
    them: 'A second security model to maintain',
    strata: "Inherits the user's row level security",
  },
  {
    axis: 'Scope',
    them: 'One chat box over the whole warehouse',
    strata: 'A purpose built agent per project',
  },
];

const absent = [
  'Hand an LLM raw SQL access to your warehouse to guess with.',
  'Ship one general chatbot and hope it rediscovers your model on every task.',
  'Bolt AI on as a layer that bypasses your governance.',
];

const gained = [
  'Agents that return correct answers, because they cannot query around the model.',
  'Context that is real and current, not a paragraph describing your business.',
  'One security model, inherited by every human and agent alike.',
];
---

<Base
  title="Agents your experts build, not a chatbot you bolt on | Strata"
  description="Stop bolting one chatbot onto your warehouse. Your domain experts build purpose built agents the same way they build reports, grounded in live data and real semantic entities, on a governed model that makes correctness structural."
>
  <ProductHero
    eyebrow="AI Analytics"
    title="Agents your experts build, not a chatbot you bolt on"
    subhead="Everyone is bolting a chat box onto their warehouse and watching it guess joins and return confident wrong answers. Strata takes a different path. Your domain experts build purpose built agents the same way they build reports, grounding each one in live data and the real entities from your governed model. Every question starts with your project's context already in hand, never from ground zero."
    diagram="agentloop"
    alt="The agent validation loop: an agent proposes a partial query, Strata validates and responds, repeating into a grain safe answer"
    ctaLocation="aa-hero"
  />

  <!-- THE COLD CHATBOT PROBLEM -->
  <!-- Ink-scoped (.dark): the problem beat, set apart from the warm narrative. -->
  <section class="dark border-b border-border bg-background">
    <div class="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-3xl text-center">
        <h2 class="font-display text-3xl font-bold tracking-tight sm:text-4xl">
          A chatbot on your warehouse starts from <span class="text-primary">zero</span>, every time
        </h2>
        <p class="mt-5 text-lg leading-relaxed text-muted-foreground">
          Point an LLM at a hundred inconsistent tables and it guesses the joins, guesses the grain, and
          hands back a number that is fluent, confident, and quietly wrong. No prompt fixes it, because
          the model has no way to know the join was invalid. It is the top reason AI over data pilots
          stall.
        </p>
      </div>

      <div class="mx-auto mt-14 grid max-w-5xl gap-6 lg:grid-cols-2">
        <div class="rounded-xl border border-border bg-surface p-7">
          <h3 class="font-display text-xl font-bold tracking-tight">It guesses, then sounds sure</h3>
          <p class="mt-3 leading-relaxed text-muted-foreground">
            An LLM writing SQL over a messy schema joins facts at the wrong grain and double counts. The
            failure is not a crash you can catch. It is a plausible number that is wrong, and nothing in
            the answer tells you so.
          </p>
        </div>
        <div class="rounded-xl border border-border bg-surface p-7">
          <h3 class="font-display text-xl font-bold tracking-tight">It starts cold, every task</h3>
          <p class="mt-3 leading-relaxed text-muted-foreground">
            A single chat box over the whole warehouse has no project context. It rediscovers what is
            relevant on every question, thrashing across tables with no memory of what you were actually
            trying to do.
          </p>
        </div>
      </div>

      <div class="mx-auto mt-12 max-w-3xl text-center">
        <p class="font-display text-xl font-semibold leading-relaxed text-foreground sm:text-2xl">
          Accuracy cannot come from better prompting. It has to be a property of the system the agent
          queries.
        </p>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section id="walkthrough" class="scroll-mt-20 border-b border-border">
    <div class="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-3xl text-center">
        <p class="font-display text-sm font-semibold uppercase tracking-wider text-primary">
          How it works
        </p>
        <h2 class="mt-3 font-display text-3xl font-bold tracking-tight sm:text-4xl">
          A purpose built agent, grounded and governed
        </h2>
        <p class="mt-5 text-lg leading-relaxed text-muted-foreground">
          Not one chatbot over everything. A focused agent your experts build, holding the exact data and
          context its job needs, on a model that will not let it guess a wrong answer.
        </p>
      </div>
      <div class="mt-20">
        <DayInLife steps={steps} />
      </div>
    </div>
  </section>

  <!-- TRUSTWORTHY BY CONSTRUCTION -->
  <section class="border-b border-border bg-surface">
    <div class="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-2xl text-center">
        <h2 class="font-display text-3xl font-bold tracking-tight sm:text-4xl">
          Trustworthy by construction
        </h2>
        <p class="mt-5 text-lg leading-relaxed text-muted-foreground">
          The same governed model that makes your experts fast is what makes their agents safe.
        </p>
      </div>
      <div class="mt-14">
        <CapabilityGrid cards={capabilities} />
      </div>
    </div>
  </section>

  <!-- HEAD TO HEAD -->
  <section class="border-b border-border">
    <div class="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-3xl text-center">
        <h2 class="font-display text-3xl font-bold tracking-tight sm:text-4xl">
          Two ways to put AI on your data
        </h2>
        <p class="mt-5 text-lg leading-relaxed text-muted-foreground">
          A chatbot bolted onto the warehouse, or a purpose built agent grounded in a governed model.
        </p>
      </div>
      <div class="mt-14">
        <OperatorTable
          axisHeader="When you put AI on your data"
          columnLabel="Them"
          rows={compareRows}
        />
      </div>
    </div>
  </section>

  <!-- THE TRADEOFF -->
  <section class="border-b border-border bg-surface-2">
    <div class="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-3xl text-center">
        <h2 class="font-display text-3xl font-bold tracking-tight sm:text-4xl">
          What we don't do, <span class="text-primary">on purpose</span>
        </h2>
        <p class="mt-5 text-lg leading-relaxed text-muted-foreground">
          An agent is only as trustworthy as the system it queries. So we refuse to build the parts that
          let it guess its way to a confident wrong answer.
        </p>
      </div>
      <div class="mt-14">
        <TradeoffPanel
          absent={absent}
          gained={gained}
          absentTitle="What Strata does not do"
          gainedTitle="What it protects"
        />
      </div>
      <p class="mx-auto mt-12 max-w-3xl text-center font-display text-xl font-semibold leading-relaxed text-foreground sm:text-2xl">
        Correctness is not a prompt. It is a property of the model the agent queries, and we refuse to
        ship anything that lets an agent route around it.
      </p>
    </div>
  </section>

  {featuredStudy && <CaseStudySpotlight study={featuredStudy} />}

  <!-- CLOSING CTA -->
  <section class="border-t border-border bg-strata-ink">
    <div class="mx-auto max-w-3xl px-4 py-20 text-center sm:px-6 lg:px-8">
      <h2 class="font-display text-3xl font-bold tracking-tight sm:text-4xl">
        Put an agent on your data, without the wrong answers
      </h2>
      <p class="mt-4 text-muted-foreground">
        Bring the question your last chatbot got confidently wrong. We will build an agent that gets it
        right.
      </p>
      <CtaButton location="aa-closing" class="mt-8" />
    </div>
  </section>

  <VideoLightbox />
</Base>
